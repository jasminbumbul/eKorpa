@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Reset autentifikacijskog ključa";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h4>@ViewData["Title"]</h4>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Ako resetujete svoj autentifikacijski ključ, Vaša autentifikacijska aplikacije možda neće raditi prije nego ih namjesite.</strong>
    </p>
    <p>
        Ovaj proces onemogući 2FA sve dok ne potvrdite Vašu autentifikacijsku aplikaciju.
        Ako ne dovršite konfiguraciju vaše autentifikacijske aplikacije možete izgubiti pristup računu.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post" class="form-group">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Resetuj autentifikacijski ključ</button>
    </form>
</div>