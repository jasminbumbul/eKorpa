@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Lični podaci";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h4>@ViewData["Title"]</h4>

<div class="row">
    <div class="col-md-6">
        <p>Vaš račun sadrži lične podatke koje ste nam dali. Ova stranica vam omogućuje da skinete ili izbrišete te podatke.</p>
        <p>
            <strong>Brisanjem ovih podataka trajno brišete svoj račun, i ovo se ne može poništiti.</strong>
        </p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post" class="form-group">
            @Html.AntiForgeryToken()
            <button class="btn btn-primary" type="submit">Download</button>
        </form>
        <p>
            @Html.AntiForgeryToken()
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-primary">Izbriši</a>
        </p>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}